---
import type { Faq } from "../lib/types";

interface Props {
  faq: Faq;
}

const { faq } = Astro.props;
---

<div class="mb-8">
  <h3 class="font-bold text-2xl mb-2">{faq.question}</h3>
  <p>{faq.answer}</p>
</div>
